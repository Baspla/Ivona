/*var fs = require('fs'),
    fabric = require('fabric').fabric;*/

module.exports.generateImage = function generateImage(text,name) {


    /*var canvas = new fabric.StaticCanvas(null, { width: 800, height: 600 });
    canvas.setBackgroundColor("#000f");
    var text = new fabric.Text('Hello world', {
        left: 100,
        top: 100,
        fill: '#f55f',
        angle: 15,
        fontSize: 48
    });
    canvas.add(text);
    var out = fs.createWriteStream("resources/temp/justThings.png");
    var stream = canvas.createPNGStream();
    stream.on('data', function(chunk) {
        out.write(chunk);
    });*/
};
