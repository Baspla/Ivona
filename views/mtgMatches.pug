extends layout

block variables
    - var title = "Magic Matches"

block breadcrumbs
    li.breadcrumb-item
        a(href="/") Home
    li.breadcrumb-item
        a(href="/user/") Dashboard
    li.breadcrumb-item
        a(href="/user/magic") Magic
    li.breadcrumb-item.active(aria-current="page") Matches

block content
    .container-fluid.bg-light.py-4
        h2 Matches