extends layout

block variables
    - var title = "Profil"

block breadcrumbs
    li.breadcrumb-item
        a(href="/") Home
    li.breadcrumb-item.active(aria-current="page") Profil

block content
    .container-fluid.bg-light.py-4
        h2 #{session.user.name}