extends layout

block variables
    - var title = "Profil bearbeiten"

block breadcrumbs
    li.breadcrumb-item
        a(href="/user") Dashboard
    li.breadcrumb-item
        a(href="/user/profil") Profil
    li.breadcrumb-item.active Profil bearbeiten

block content
    .container-fluid.bg-light.py-4
        h2 Profil Bearbeiten
        form
            input.mr-3(type="text" name="name" placeholder="Nutzername" required value = session.user.name)
            button.btn.btn-primary(type="submit") Nutzername ändern